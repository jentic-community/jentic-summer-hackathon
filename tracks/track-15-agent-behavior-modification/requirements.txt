# Core dependencies
pydantic>=2.0.0
python-dateutil>=2.8.0
typing-extensions>=4.0.0

# Standard Agent dependencies (from parent project)
# These would typically be inherited from the main project

# NLP and LLM dependencies for policy parsing
openai>=1.0.0
litellm>=1.0.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Development dependencies
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
